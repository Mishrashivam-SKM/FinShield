// components.js — Reusable UI component helpers
// Badge renderer and table builder are embedded in app.js for simplicity
// This file exists for SRD compliance — helpers are in app.js badge() and $()
